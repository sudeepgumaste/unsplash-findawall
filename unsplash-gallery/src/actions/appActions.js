export const setPopupState = (trigger) => ({
    type: 'SET_POPUP_STATE',
    trigger
})

export const setImageObject = (image) => ({
    type: 'SET_IMAGE_OBJECT',
    image
})

export const setShowNavSearch = (trigger) => ({
    type: 'SHOW_NAV_SEARCH',
    trigger
})